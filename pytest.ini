[pytest]
testpaths = tests
markers =
    smoke: critical path tests, run after every deployment
    regression: full suite, run before release
addopts = -v --reruns 2 --reruns-delay 1 --html=report.html --self-contained-html